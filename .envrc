# nix print-dev-env clobbers SHELL with stdenv's readline-less build bash,
# which mangles prompts and breaks anything that spawns $SHELL. Keep the
# real one across the flake load.
real_shell=${SHELL:-}
use flake
if [ -n "$real_shell" ]; then
  export SHELL=$real_shell
fi
